<h1>Producer::Formulas#index</h1>
<p>Find me in app/views/producer/formulas/index.html.erb</p>

<ul>

  <% @formulas.each do |formula| %>
    <li>
      <ul>
        <li><%= t "formula_categories.#{formula.category}" %></li>
        <li><%= formula.name %></li>
        <li><%= formula.description %></li>
        <li><%= humanized_money_with_symbol formula.price %></li>
      </ul>
    </li>
  <% end %>

</ul>


<%= link_to "Ajouter une formule", new_producer_period_formula_path %>
